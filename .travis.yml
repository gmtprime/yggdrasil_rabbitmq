language: elixir
elixir:
  - 1.6.5
otp_release:
  - 20.3.8
services:
  - rabbitmq
before_script:
  - MIX_ENV=test mix deps.get
  - MIX_ENV=test mix deps.compile
  - MIX_ENV=test mix compile
script:
  - MIX_ENV=test mix test
